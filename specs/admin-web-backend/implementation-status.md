# 后端实施状态

更新时间：2026-06-26

## 主线回填

- 后端实现线程：`019eff3e-0424-78d1-948f-da646fd338b2`。
- 后端工程位置：`backend`。
- 当前交付判断：第一版可运行 Spring Boot 后端已完成，可作为 `admin-web` 与旧 H5 `/auth/*` 的联调基线。
- 前端对接线程：`019eff31-166d-7822-84a0-f530ddf5970e`。
- 前端对接基准：以后端实际 Controller/DTO、`backend/README.md` 和本文档为准；原接口文档仅作为业务语义参考。
- 主线复验：2026-06-26 在非沙箱环境执行 `./mvnw -q clean verify` 通过，7 项测试 0 失败。
- 说明：受限沙箱内曾触发 Mockito/ByteBuddy JVM 自附加限制，不作为业务验证结果。

## 已落地

- 新建 `backend` Spring Boot 工程与 Maven Wrapper。
- MySQL/Flyway 数据模型、字典和权限菜单。
- 管理员 JWT、权限标识和 `org_id` 数据范围。
- 课程、课次、小鹅通来源解析开发适配器。
- 学员、多课程授权、唯一主课程、手机号加密/hash/脱敏。
- Excel 模板、校验令牌、确认导入和脱敏导出。
- 本地临时/正式文件存储、一次性令牌和鉴权下载。
- 资料批量新增、物理作业题目结构及小题分导出。
- 提分宝 ZIP 安全解析、学员匹配、批次和学生个人文件发布。
- 旧 `/auth/*` 请求头与字段兼容。
- Swagger/OpenAPI、Dockerfile、Compose 和环境变量示例。

## 自动化测试

- Spring 上下文与 Flyway 迁移。
- 管理员登录和 JWT 签发。
- 课次重叠拒绝。
- 多课程唯一主课程及主课程切换。
- 手机号数据库唯一性。
- 旧 H5 `token` 请求头、手机号绑定和主课程返回。
- ZIP 路径穿越拒绝。

## 上线前遗留

1. 根据现网小鹅通 API 或 SDK 实现正式客户端；当前 `stub` 不可用于生产开课。
2. 接入现有若依用户/角色体系（若后续提供）；当前独立工程已使用 BCrypt 初始管理员。
3. 生产多实例部署时将本地文件存储替换为 COS、OSS 或 MinIO。
4. 使用真实 MySQL 8、Redis 和反向代理完成预发布联调。
5. 微信内下载不能使用 `bypassToken=true`；需由 H5 改为可携带令牌的下载方式，或增加短时一次性签名链接。
6. 增加高并发压测、真实 500MB ZIP 压测及恶意文件扫描。
